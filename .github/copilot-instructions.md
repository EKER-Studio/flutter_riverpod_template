# GitHub Copilot Instructions

@AGENTS.md

Follow the rules defined in `AGENTS.md` above, and in `agents_project.md` if present in the repository root,
before making any changes. These are the single source of truth for architecture, coding conventions,
development workflow, resource disposal, verification pipeline, and testing rules.

Do not introduce Copilot-specific overrides unless explicitly required.
